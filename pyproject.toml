[project]
name = "hello-manim"
version = "0.1.0"
description = "A minimal Manim Community animation project"
readme = "README.md"
authors = [
    { name = "Beto", email = "betomoedano@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "manim>=0.21.0",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
